//console.log('estou em --> módulo.js');

const sobrenome = 'Matuck';

const calculoMaluco = (numero, valorIncrementado) =>{
    return (numero * valorIncrementado + 2) / 10
}

module.exports = calculoMaluco;